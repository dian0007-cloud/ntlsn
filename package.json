{
  "name": "ntlsn",
  "version": "1.0.0",
  "private": true,
  "description": "NTLSN — National Teaching and Learning Sector Navigator. Static site + Netlify serverless backend.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test netlify/functions/test/*.test.js",
    "build": "echo 'NTLSN is a static site — no build step'",
    "test:e2e": "playwright test",
    "lhci": "lhci autorun"
  },
  "dependencies": {
    "@netlify/blobs": "^8.2.0"
  },
  "devDependencies": {
    "@lhci/cli": "^0.13.0",
    "@playwright/test": "^1.61.1",
    "axe-core": "^4.12.1",
    "playwright": "^1.61.1"
  }
}
