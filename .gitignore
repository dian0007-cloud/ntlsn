.DS_Store
node_modules/
*.backup-*.html
*.log

# stray editor/backup snapshots
*.bak-*

# local secrets — never commit (functions read these from Netlify env, not files)
.env
.env.*
.netlify/

# Source rebuild (TASKS.md Epic 1) build output — src/node_modules is already
# covered by the node_modules/ rule above
src/dist/

# Playwright + Lighthouse CI artifacts (regenerated each run)
test-results/
.lighthouseci/
playwright-report/
